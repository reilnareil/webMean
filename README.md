# Angular 12 Node.js MongoDB: CRUD Application

## Project setup

### Node.js Server
```
cd node-express-server
npm install
```
Run `node server.js`

### Angular book
```
cd book-frontend
npm install
```
Run `ng serve --port 8081`. Navigate to `http://localhost:8081/`.